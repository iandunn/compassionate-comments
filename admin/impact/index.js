/**
 * WordPress dependencies
 */
import { Fragment } from '@wordpress/element';

/**
 * Internal dependencies
 */


/**
 * Render the view for the main interface.
 *
 * @param {Array} props
 *
 * @return {Element}
 */
export function Impact( { foo, bar } ) {
	// if this ends up needing state, then create controller/view files like main/settings components

	return (
		<Fragment>
			Under development.
		</Fragment>
	);
}
